const styles = (theme) => ({
    timer: {
        display: 'flex',
        alignItems: 'center',
        flex: 1     
      },
  
      timerText: {
        flexGrow: 1,
        textAlign: 'center',
        fontSize: '2.5rem',
        color: '#283593'
      },
    })

export default styles;